<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/ipost_write_root_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:animateLayoutChanges="true"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <EditText
        android:id="@+id/ipost_edt"
        style="@style/styleEdt"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="start"
        android:hint="Write the message you want to post on G+"
        android:inputType="textCapSentences|textMultiLine"
        android:lines="6"
        android:maxLines="15"
        android:minLines="8"
        android:scrollbars="vertical"
        android:singleLine="false" />

    <Button
        android:id="@+id/ipost_send"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_gravity="center"
        android:layout_margin="4dp"
        android:background="@drawable/button"
        android:padding="4dp"
        android:text="Post" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/button"
        android:gravity="center"
        android:text="This will post your message in GooglePlus using an interactive Post containing your message, a link to that application and a link to the project page."
        android:visibility="visible" />

</LinearLayout>